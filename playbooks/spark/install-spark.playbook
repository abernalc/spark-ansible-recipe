---
- name: Playbook to install spark
  hosts: spark-servers
  remote_user: root
  gather_facts: no
  pre_tasks:
    - name: 'install python'
      raw: sudo apt-get -y install python
  roles:
    - spark
